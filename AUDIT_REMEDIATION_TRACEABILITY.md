# KingPepe Audit Remediation Traceability

Audit commit: `ae134339eb6f136d9516e8fe40b8b1be9b632b7b`

Remediation branch: `audit-remediation`

Current remediation head: `c6658662670c6d543f29408425f26e38bb8cc0b0`

| Finding ID | Severity | File and line | Current status | Required correction | Test proving the correction | Commit containing the correction |
|---|---|---|---|---|---|---|
| KP-AUD-001 | Medium | `src/qt/bitcoingui.cpp:301-306` | Fixed; source no longer contains the unapproved inactivity timer or `setWalletLocked(true)` timer path. | Keep GUI visual-only; no new wallet lock behavior. | `remediation-output/KP-AUD-001/source-grep-autolock.log`; `build-kingpepe-qt.log`; `build-qt-and-qt-tests.log`; `ctest-qt-tests.log` (`test_kingpepe-qt` passed). | `b402651` |
| KP-AUD-004 | Medium | `src/kernel/chainparams.cpp:114-115` | Documented; no code change (no independently-operated, live, ownership-documented second DNS seed is verifiable, and adding an arbitrary peer IP is disallowed "invent-a-server"). Single seed + empty `vFixedSeeds` + one RFC 4193 ULA leak recorded. | Documented the seed-risk and a safe operator deployment plan (≥2 vetted DNS seeders, fix ULA leak, generate vetted `vFixedSeeds`, coordinated release). No consensus/ports/magic/prefixes/chain-identity change. | Read-only DNS/peer verification captured in the plan (`node3.walletbuilders.com` resolution; live peer `85.10.148.5:24028` `/Kingpepe:25.0.0/`). Bootstrap smoke deferred to operator rollout. | `c955edc` (`remediation-output/KP-AUD-004-DNS-SEED-PLAN.md`) |
| KP-AUD-005 | Medium | Bitcoin-economics / address / key assumptions across the functional suite | Partially fixed; ported & individually verified green: `wallet_backup.py` (fixed a real fund-depletion bug in the earlier port), `mempool_spend_coinbase.py`, `wallet_coinbase_category.py`, `wallet_orphanedreward.py`, `wallet_simulaterawtx.py`, `rpc_generate.py`, `rpc_deriveaddresses.py`, `rpc_decodescript.py` (+ regenerated `data/rpc_decodescript.json`), `rpc_signmessagewithprivkey.py`, `rpc_signrawtransactionwithkey.py`. Framework fix: `test_framework/wallet_util.py` WIF version byte 239→191. Remaining failures are dominated by (a) shared-cache balance asymmetry from the block-1 premine (needs per-test clean-chain rescale) and (b) further address/key vectors — classified in the final report. | Port Bitcoin assumptions using node-generated KingPepe vectors (subsidy 3/halving-150, WIF 0xBF/191, base58 64/63, HRP rkpepe) and preserve assertions; never weaken. | Each ported test run individually to `Tests successful`; full-suite counts in `remediation-output/functional-final.log`. | `9e315ff`,`e0d15ef` (earlier); `40d7b4d`,`c626a37`,`a7d4de4`,`ec7ffe8`,`c665866` (this pass); remaining pending |
| KP-AUD-007 | Medium | `.github/workflows/ci.yml`; `.github/workflows/gui-build.yml`; `.github/actions/cache/restore/internal/action.yml`; `.github/actions/cache/save/internal/action.yml`; `.github/actions/configure-docker/action.yml` | Fixed; every third-party action `uses:` pinned to an immutable full commit SHA with a `# vN` comment; `ci.yml` gained top-level least-privilege `permissions: contents: read`; `gui-build.yml` already had `permissions: contents: read`. Release/CI jobs unchanged. | Pin third-party actions to immutable SHAs and add least-privilege permissions. | YAML parse of all changed workflow/action files (valid); grep confirms no mutable `@vN`/`@main` third-party tags remain and explicit `permissions:` present. | `71e99d7` |
| KP-AUD-002 | Low | `src/qt/walletview.cpp:207-224` | Fixed; the dashboard now seeds its sync status from `node().getVerificationProgress()` via the existing `FormatDashboardSyncProgress/Status` helpers instead of an unconditional `100% / Synced`. | Replace initial hard-coded `100% / Synced` with real model state. UI-only. | Compiles and links clean in `kingpepe-qt.exe` (MSVC/Qt 6.8.1, `remediation-output/build-qt-aud002-003.log`). | `1cdc367` |
| KP-AUD-003 | Low | `src/qt/settingspage.cpp:95-101`; `src/qt/bitcoin.cpp:552-554` | Fixed; theme is read (bitcoin.cpp) and written (settingspage.cpp, both buttons) through one explicit fixed QSettings scope `QSettings(QAPP_ORG_NAME, QAPP_APP_NAME_DEFAULT)`, so it round-trips across restarts/networks. No new persistent OptionsModel setting. | Move theme read/write into one consistent UI-only QSettings scope without changing OptionsModel. | Compiles and links clean in `kingpepe-qt.exe`. | `1cdc367` |
| KP-AUD-006 | Low | `.github/workflows/ci.yml:33,222,326` | Fixed in remediation base; tag `v1.1.0` had bad runner labels, branch base uses GitHub-hosted labels. | Keep `ubuntu-latest` and `windows-2025` runner labels. | Diff `v1.1.0..remediation-base`; workflow runner-label grep. | `ad36b8d` |
| KP-AUD-008 | Low | `ci/lint/01_install.sh:28,51,56`; `ci/test/01_base_install.sh:27,60,83,97`; `ci/test/03_test_script.sh:50,61` | Documented; not changed. Inherited Bitcoin-Core Linux-CI Docker scripts (apt/clone/OSX-SDK) that cannot be executed or verified on this Windows host; an unverified edit could break upstream CI. Exact SHA-pin + SHA-256 `verify_download()` fix specified for a Linux CI run. | Add checksum/immutable-commit verification where low-risk on a Linux runner; avoid breaking upstream CI. | To be validated on a Linux CI runner (not verifiable on Windows). Documented in the Low-findings note. | `7768afd` (`remediation-output/LOW-FINDINGS-008-009-010-012.md`) |
| KP-AUD-009 | Low | Live website/CDN configuration | External/open; not repository source. Re-verified 2026-07-31: `http://kingpepe.net/` → 200 (no redirect); HTTPS+HSTS valid. | Enable "Always Use HTTPS" (edge 301 http→https) on Cloudflare/origin; keep HSTS. | `curl -I http://kingpepe.net/` must return 301/308 to `https://kingpepe.net/`. | `7768afd` (documented; fix is external) |
| KP-AUD-010 | Low | `https://kingpepe.net/openapi.json` (kingpepe-explorer service) | External/open; not this repository. Re-verified 2026-07-31: `servers` still lists `http://127.0.0.1:8080` alongside prod. Documentation drift only; no secret/raw-RPC exposure. | In the explorer source, remove/gate the localhost `servers` entry and represent all live `/api/v1/*` routes. | Fetch OpenAPI and compare to live route inventory. | `7768afd` (documented; fix is in explorer repo) |
| KP-AUD-011 | Informational | `src/qt/bitcoinunits.cpp:24-36`; `src/qt/bitcoinstrings.cpp:12`; test target names | Open informational. | Clean internal branding only where compatibility-safe; do not rename serialized settings or consensus symbols blindly. | Branding lint plus amount-format and wallet compatibility tests. | Pending |
| KP-AUD-012 | Low | `test/lint/lint-files.py` results | False positive / environment; no repo defect. The flagged files DO contain `#!/usr/bin/env python3` and git HEAD stores them with LF; the 406 "missing shebang" hits are caused by the Windows working-copy CRLF conversion (`...python3\r`) tripping lint-files' exact first-line match. On an LF/CI checkout the lint passes. | None in-repo (shebangs already present). Optional: `* text=auto eol=lf` in `.gitattributes` (deferred to avoid renormalization churn). Do not "fix" already-correct shebangs. | `git show HEAD:<file>` shows correct LF shebang; lint passes on LF checkout. | `7768afd` (documented) |
| KP-AUD-013 | Informational | `vcpkg.json`; audit build logs | Environment issue; no source change planned in first remediation pass. | Use full vcpkg checkout at pinned baseline for reproducible manifest builds. | Clean manifest-mode configure on fresh/full vcpkg checkout. | Environment |
